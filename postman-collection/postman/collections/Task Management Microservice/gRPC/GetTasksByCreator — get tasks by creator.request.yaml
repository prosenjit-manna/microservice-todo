$kind: http-request
description: |-
  **gRPC — TaskService.GetTasksByCreator**

  Proto package: `task`  
  Service: `TaskService`  
  Method: `GetTasksByCreator`  
  Server URL: `localhost:50052`  
  Proto file: `src/proto/task.proto`

  Returns all active tasks created by a given user ID.

  **Request message:**
  ```json
  { "creatorId": "<mongodb-object-id>" }
  ```

  **Response message:**
  ```json
  {
    "tasks": [
      {
        "id": "...",
        "title": "My first task",
        "status": "todo",
        "priority": "medium",
        "creatorId": "...",
        ...
      }
    ]
  }
  ```
url: "{{grpc_url}}"
method: POST
headers:
  - key: Content-Type
    value: application/json
  - key: X-Grpc-Note
    value: "Use Postman gRPC client: package=task, service=TaskService, method=GetTasksByCreator"
    disabled: true
body:
  type: json
  content: |-
    {
      "creatorId": "{{user_id}}"
    }
order: 3000
